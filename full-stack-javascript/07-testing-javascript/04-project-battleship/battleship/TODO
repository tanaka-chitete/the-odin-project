Let the user change missile placement
	Let them tag a location
	Let them clear the tag
	Let them fire at the tag
Let the user change ship placement
	Let them rotate each ship
	Let them move each ship
	Let them clear all ships
Let the users view their allocation's
	Let them view Player 1's allocation's health
	Let them view Player 2's allocation's health
Style the UI
	Display ships on-top of board borders
	Style the logo
	Style the "Message" view
	Style the "Start" controls
	Style the "Placement" controls
	Style the "Battle" controls
Miscellaneous
	If allocation is exhausted, don't let the user drag a ship onto the board
	Remove "click" event listener from cells which have already been clicked
	Disable "Launch" button if no cell is tagged
	Disable "End Preparation" button if board is not full