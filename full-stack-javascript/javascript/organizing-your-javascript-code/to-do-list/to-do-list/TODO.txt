Load tasks when the user opens the app
Refactor View.createElement() to only receive object as parameter. For example,
{
	"tag": "div",
	"attributeNameToValue": {...},
	"textContent": {...}
}
Display artwork for empty projects
In code, refer to tasks as being 'created' as opposed to 'added'